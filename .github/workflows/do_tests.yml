name: do stuff

on:
  create:
  delete:
  push:
  pull_request:
    types: [closed, synchronize, opened, reopened]

jobs:
  do_stuff:
    name: Do stuff
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ github.event_name }}
