name: do another stuff

on:
  workflow_call:

env:
  PROJECT_NAME: ${{ secrets.PROJECT_NAME }}

jobs:
  do_another_stuff:
    name: Do another stuff
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ github.event_name }}
      - run: echo ${{ secrets.PROJECT_NAME }}
